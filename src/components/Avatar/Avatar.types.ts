export type MaskProps = {
    bgColor?: string,
    shapeSrc: string,
};

type AvatarProps = {
    height?: string,
    bgColor?: string,
    shapeSrc: string,
    imageSrc: string,
    paddingTop?: string,
    paddingBottom?: string,
};

export default AvatarProps;
