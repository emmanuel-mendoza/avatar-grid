export type MaskProps = {
    bgColor?: string,
    clipShape: string,
};

type AvatarProps = {
    cmpHeight?: string,
    bgColor?: string,
    clipShape: string,
    imgSrc: string,
    marginT?: string,
    marginB?: string,
};

export default AvatarProps;
